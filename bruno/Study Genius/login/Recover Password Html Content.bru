meta {
  name: Recover Password Html Content
  type: http
  seq: 5
}

post {
  url: http://{{host}}/api/v1/password-recovery-html-content/{email}
  body: none
  auth: bearer
}

auth:bearer {
  token: {{access_token}}
}
