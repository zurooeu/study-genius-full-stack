meta {
  name: Chat Continue Conversation
  type: http
  seq: 2
}

post {
  url: http://{{host}}/api/v1/chat/{conversation_id}
  body: json
  auth: bearer
}

auth:bearer {
  token: {{access_token}}
}
